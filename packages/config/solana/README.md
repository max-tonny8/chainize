# `@chainized/config/solana`

Auto-generated directory which contains local copy of wallets that are used for deployment of `anchor` contracts from `@chainized/spl` package.

- `config.yml`
- `id.json`
